Original prompt: ブラウザで対戦できるカードゲームを目指す。ユニットフィールド、コアフィールド、サモンフィールド、メインデッキ、タクトゾーン、墓地、ストラクトデッキ、ストラクトゾーン、ワイルドゾーン、グランドゾーンの概念と、7種類の資源、コアカード、ユニットカード、ストラクトカード、ユニット移動・攻撃ルールを基礎システムとして作る。カード画像は後でユーザーが用意する。能力や効果を定義しやすい構造にする。

## Progress

- 2026-07-01: Confirmed 王城ノーベルグ has no recurring income of any kind and now starts with funds 8 only; removed its initial magic 1. Added explicit regression assertions for funds income 0, initial funds 8, and initial magic 0.
- 2026-07-01: Fixed five gameplay issues: 王城ノーベルグ now has zero turn income; 第二墓標 revives only into its controller's battle row; deck/hand selection overlays and text summaries are redacted from the opponent; [魂] now prompts for an optional exact replacement count before play; and 攪乱工兵's adjacent effect protection remains valid through simultaneous damage/destruction batches.
- Added focused regressions in `tests/keyword-tests.mjs`. Focused browser checks pass, and the [魂]/private-selection overlays were visually inspected. The full legacy suite currently stops at the pre-existing mobile movement case because row 2 / col 0 is no longer a valid unit-field cell.

- Created a static browser prototype with `index.html`, `styles.css`, and `src/main.js`.
- Implemented a data-driven card catalog, resource costs, effect registry, main decks, struct decks, hands, dumps, cores, and a 4x5 unit board.
- Implemented local hotseat play: draw, income, structure triggers, summon from hand, build structures from struct deck, play tact cards, move, retreat, attack units, counter-damage, direct core attacks, and win detection.
- Added `window.render_game_to_text` and `window.advanceTime` for Playwright verification.
- Verified summon flow with Playwright and Browser. Fixed action-cost payment order so invalid move/attack attempts do not consume resources before validation.
- Found and fixed a movement blocker bug where empty cells in the forward row were treated as enemy units.
- Added keyword ability definitions from the provided image: armor, pierce, shock, charge, mobile, multiStrike, flying, arc, legendary, alert, guard, selfDestruct, raid, immobile, noAttack, soulPay, and cleave.
- Implemented representative battle/phase handling for those keywords: damage reduction, armor ignore, resting on damage, electric attack surcharge, once-per-turn mobile movement, multi-attack rest timing, flying attack/counter restrictions, arc range/no counter, end-turn alert unrest, guard protection, self-destruct splash, raid second-row summon, movement/attack lockouts, soul-based magic payment, and cleave splash.
- Added demo units using the new keywords and included keyword labels in card rendering and `render_game_to_text`.
- Added `Ore Mine` and expanded the Struct Deck display to two rows so all current structures can be inspected and selected.
- Re-ran Playwright summon/attack checks with no console errors, and reloaded the prototype in the in-app Browser.
- Added `window.__twcg.testing` helpers for deterministic browser-state tests: reset, placeUnit, selectUnit, addHandCard, summonFromHand, playStruct, attack, move, retreat, endTurn, and summary.
- Added `tests/keyword-tests.mjs`, which verifies armor, pierce, shock, flying restrictions, mobile movement, guard protection, arc plus cleave, self-destruct splash, raid summon placement, and alert unrest without relying on click choreography.
- Verified `node tests\keyword-tests.mjs` and the existing Playwright attack choreography; both pass with no console errors.
- Added fixture cards for the remaining keyword coverage: Charged Lancer, Rapid Gunner, Bunker, Soul Mage, and Legendary Ace.
- Fixed charge attack payment so the ordinary act cost and added electric cost are paid atomically; failed charge payment no longer spends partial resources.
- Expanded `tests/keyword-tests.mjs` to cover charge, atomic charge failure, multiStrike, immobile, noAttack, soulPay, and legendary duplicate rejection. The suite now covers 16 keyword cases.
- Reloaded the in-app Browser after the changes and confirmed the canvas page is available at `http://127.0.0.1:5173/`.
- Added an effect queue and pending target model. Abilities now enqueue, immediate effects resolve in order, and targeted effects keep a pending target until a legal board target is selected.
- Added `Precision Strike`, a targeted tact card that waits for an enemy unit target, remains in the Tact Zone while pending, damages the chosen unit, then moves to the Dump Zone.
- Added state output for `pendingTarget`, `effectQueueCount`, and each player's `tactZone`.
- Expanded `tests/keyword-tests.mjs` to cover immediate tact resolution, targeted tact waiting, valid target resolution, invalid target rejection, Tact Zone retention, and Dump Zone cleanup. The suite now covers 20 cases.
- Added optional card image support. Card definitions can now set `image` or `imageUrl`, images are cached and cropped into the card art area, and missing/failed images fall back to the existing drawn placeholder.
- Added `assets/cards/README.md` documenting the expected image placement and card definition field.
- Added `image` to hand and board entries in `render_game_to_text` so image-backed cards are visible in test output.
- Verified the no-image fallback with keyword tests, Playwright attack choreography, screenshot inspection, and an in-app Browser reload.

- Added first-pass Wild and Grand card support. `Hidden Supply` can be paid from hand into `wildZone` face down, and `Grand Mandate` can be paid from hand into `grandZone` face up.
- Added Wild/Grand side-panel badges and exposed both zones through `render_game_to_text`.
- Added testing helpers `playWildFromHand` and `playGrandFromHand`, plus regression cases for face-down Wild placement and face-up Grand placement. `tests/keyword-tests.mjs` now covers 22 cases.
- Verified `node --check src/main.js`, `node tests/keyword-tests.mjs`, and the Playwright attack choreography. Inspected the screenshot and fixed a Wild/Grand badge overlap with the Struct Zone heading.
- Added initial neutral cards: `軽歩兵`, two `小さな野戦砲` variants with different act costs, and `ありふれた装甲車`.
- Added `faction`, `tags`, and optional `variant` fields to these card definitions, displayed unit tags on cards, and exposed faction/tags/variant in `render_game_to_text`.
- Placed the new initial cards at the front of the demo main deck so they appear in the opening hand.
- Added `initial_cards_match_spec` assertions to `tests/keyword-tests.mjs`. The suite now covers 23 cases and verifies the supplied costs, act costs, tags, keywords, and ATK/HP.
- Verified syntax checks, `node tests/keyword-tests.mjs`, Playwright attack choreography, screenshot inspection, and in-app Browser reload with no console errors.
- Removed the old demo units from the default main deck. They now stay only as `fixture: true` internal cards for keyword regression tests.
- Renamed the old demo unit display names to Japanese: 民兵分隊, 装甲車, 魔導砲兵, 衝撃歩兵, 偵察機, 戦列守備兵, 奇襲バイク, 自爆ドローン, 帯電槍兵, 連撃射手, 掩体壕, 魂術師, 伝説のエース.
- Added `default_deck_excludes_demo_fixtures` to ensure opening hand and default deck do not include fixture units. The test suite now covers 24 cases.
- Verified syntax checks, `node tests/keyword-tests.mjs`, Playwright attack choreography, screenshot inspection, and in-app Browser reload with no console errors after the rename/deck cleanup.
- Renamed visible structure cards to Japanese: 町, 資源林, 鉱山, 精製所, 発電所, 魔力井戸.
- Added `struct_cards_use_japanese_names` to lock the Struct Deck names. The test suite now covers 25 cases.
- Verified syntax checks, `node tests/keyword-tests.mjs`, Playwright attack choreography, screenshot inspection, and in-app Browser reload with no console errors after the structure rename.
- Renamed visible tact/wild/grand cards to Japanese: 野戦命令, 精密攻撃, 隠匿補給, 大号令. Updated their visible text where it was still English.
- Updated targeted tact and grand-zone tests to expect the Japanese names, and added an assertion that the default deck contains the Japanese tact/wild/grand names.
- Verified syntax checks, `node tests/keyword-tests.mjs`, Playwright attack choreography, screenshot inspection, and in-app Browser reload with no console errors after the tact/wild/grand rename.

- Added an app shell on the canvas with login, home, deck builder, match lobby, and game screens.
- Added Google Identity Services script loading and a `GOOGLE_CLIENT_ID` connection point. When no client ID is configured, the Google button falls back to a local demo login state.
- Added deck builder state, localStorage deck saving, add/remove/reset deck actions, and default-deck match startup.
- Added match lobby state with local match start plus room-code create/join demo placeholders for later server synchronization.
- Added `test-actions-app-flow.json` to verify login -> deck builder -> lobby -> match start through Playwright clicks.
- Exposed `app` state and app actions through `window.__twcg`, and added `app_login_deckbuilder_match_flow` regression coverage. The test suite now covers 26 cases.
- Verified syntax checks, `node tests/keyword-tests.mjs`, app-flow Playwright choreography, screenshot inspection, and in-app Browser reload with no console errors.
- Added first real separate-browser online match support with `server.mjs`: static file serving, `/ws` WebSocket rooms, room create/join, presence, match start, and state relay.
- Wired the match lobby to online create/join, connection status, room players, and online start. Game actions now broadcast current game state while in an online match.
- Added `package.json` scripts for `npm run dev`, `npm test`, and `npm run test:online`, plus `tests/online-server-tests.mjs` and `tests/online-client-tests.mjs` for create/join/start/state relay coverage.
- Started an ngrok tunnel for local port 5174 and adjusted `ONLINE_WS_URL` so ngrok/public hosts use the same host `/ws` endpoint instead of appending `:5174`.
- Reworked Google sign-in from demo fallback to server-verified flow. `server.mjs` now exposes `/config` and `/api/auth/google`; the Google button requires `GOOGLE_CLIENT_ID`, while the old demo Google sign-in remains test-only.
- Restarted the local server with Google OAuth client ID `980630089634-1n581adgno2mirusbft1n5vt9o37na2b.apps.googleusercontent.com`; `/config` reports Google sign-in enabled locally and through the active ngrok tunnel.
- Expanded core cards to the requested template fields: name, faction, flavor, initial hand, draw count, hand limit, deck size, starting resources, and special requirements. Added four provisional cores: 前線司令部, 商業都市コア, 動員司令部, 魔導炉心.
- Core selection is now saved in `deck.core`, shown in the deck builder/lobby, applied to match start, and reflected in initial hand size and hand limit.
- Reworked the deck builder toward the reference at https://an0hit0.github.io/Deckmaker/: three-column layout for deck list, card library, and deck analysis; added library main/struct switching, grouped card counts, +/- copy controls, struct deck add/remove, JSON export, test draw, cost/resource/tag/type analysis, and copy/deck-size warnings.
- Further aligned the deck builder's structure with the Deckmaker reference while keeping the app's existing visual style: deck contents are now sectioned by card type (Unit/Tact/Wild/Grand/Struct), the library supports all/current card-type filters including Core, and library paging is in place so larger card pools remain usable.
- Added Deckmaker-style library controls without changing the visual theme: search preset buttons, popular tag filters, sort cycling, and tests for those deck-builder controls. Text free-search is still a future HTML-overlay or input-system task.
- Updated core deck-size expectations from the early 8-card prototype to a 40-60 card main deck range. Core definitions now expose `deckMin: 40` and `deckMax: 60`; the deck builder shows `current/40-60` and reports shortage/overflow warnings.
- Adjusted the core card template field `deckSize` itself to display `40〜60`, while keeping `deckMin`/`deckMax` for validation logic.
- Replaced deck-builder library paging with scrollable all-card visibility: the library now includes every registered core/main/struct card in the active filter, shows the current visible range, and supports up/down buttons plus mouse wheel scrolling.
- Removed hidden `ほか n 種` deck-list summaries. The deck list now uses a scrollable row list so all grouped main and struct deck entries can be reached with +/- controls.
- Verified `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, and a Playwright deck-builder screenshot.
- Removed the duplicate ore-only `smallFieldGun` / `小さな野戦砲` card. The remaining `smallFieldGunFuel` entry keeps the supplied display name and the `鉱1 燃1` act cost.
- Updated the default deck and initial-card regression tests after removing `smallFieldGun`; verified syntax checks, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, and a Playwright deck-builder screenshot showing 17 registered cards.
- Added food as a resource key/label and registered five proposed cards: `328-野戦対装甲魔導士隊`, `人民威力偵察部隊`, `生命妖精`, `知識`, and `神秘捕縛`.
- Implemented new effect hooks for the added cards: on-summon friendly HP buffs, on-summon draw 2, on-destroy-enemy-unit draw plus funds gain, and a provisional automatic `神秘捕縛` flow that exiles all mystic units in hand and triggers their on-summon effects twice.
- Added a player exile zone and exposed `exileCount` plus unit `maxHp` in `render_game_to_text` for testing and future UI work.
- Added the non-neutral faction deck rule: neutral cards are ignored, and a main deck may contain at most two non-neutral factions. The deck analyzer also warns when the rule is exceeded.
- Updated the default deck with `328-野戦対装甲魔導士隊`, `生命妖精`, and `知識` while keeping it within the faction limit.
- Verified `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, and a Playwright deck-builder screenshot showing 22 registered cards.
- Replaced the provisional automatic `神秘捕縛` behavior with a canvas choice UI. Playing the card now opens a modal listing mystic units in hand; cards can be toggled selected, then resolved as no selection, discard for one on-summon trigger, or exile for two on-summon triggers.
- Added `pendingChoice` state, choice details in `render_game_to_text`, and test helpers for selecting/resolving mystic capture choices. Verified both discard-once and exile-twice paths.
- Verified `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, the required Playwright client run, and a direct Playwright click flow for the `神秘捕縛` modal.
- Added named deck saving. The deck builder's save button now prompts for a deck name, stores the current deck in a saved-deck library while preserving compatibility with the previous single saved deck, and shows up to two saved decks in the analysis panel for quick loading.
- Added card full-text inspection in the deck builder. Clicking/choosing a card sets it as the selected detail card, the right panel shows a compact summary, and the `全文表示` button opens a large modal with type, faction, tags, costs, ATK/HP, keywords, generated effect text, and full card text.
- Exposed deck name, saved decks, and selected card detail through `render_game_to_text`; added regression coverage for named save/load and full-text detail extraction.
- Verified `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, the required Playwright client run, and a direct Playwright screenshot of the full-text modal.
- Renamed `knowledgeFairy` from `知識` to `知識妖精`.
- Added saved-deck selection to the match lobby. Starting from the lobby now requires selecting a named saved deck; the lobby displays saved deck buttons and loads the chosen deck before starting.
- Online battle start is now blocked until at least two room players are present. The start button shows `相手待ち` while waiting and leaves the user in the lobby with a clear opponent-required message.
- Verified `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, the required Playwright client run, and a direct Playwright screenshot of the match lobby saved-deck/opponent-wait state.
- Added server-side deck persistence. `server.mjs` now exposes `GET /api/decks?user=...` and `POST /api/decks?user=...`, storing decks in `data/decks.json` by normalized user key.
- Deck saving now remains local-first but also attempts server sync for signed-in users. Sign-in attempts to load server-saved decks, and test helpers expose sync calls for regression coverage.
- Added server/client tests proving deck save/load through the API and cross-page deck sync. Verified `node --check server.mjs`, `node --check src/main.js`, `node --check tests/*.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, and the required Playwright client run.
- Rebalanced `隠匿補給`: cost increased to `金2`, and it now places face down in Wild Zone while drawing 1 card and gaining `食料2` plus `燃料1`.
- Rebalanced `大号令`: cost increased to `金4 人2`, and it now places face up in Grand Zone while giving all friendly units `ATK+1/HP+1` and drawing 1 card.
- Added the `buffFriendlyUnitsAtk` effect hook and updated keyword regression tests for both revised cards. Verified `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, and the required Playwright deck-builder run.
- Fixed online match start synchronization by having the server echo `start` messages to every room member, including the sender, while keeping ordinary `state` updates broadcast only to the other client.
- Added a match-lobby `コードコピー` button for room codes. The copy action uses `navigator.clipboard` first, falls back to a hidden textarea copy, and finally shows the code in a prompt if browser permissions block copying.
- Added regression coverage for room-code copy and host start echo. Verified `node --check src/main.js`, `node --check server.mjs`, `node --check tests/online-client-tests.mjs`, `node --check tests/online-server-tests.mjs`, `npm run test:online`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, the required Playwright deck-builder run, and an in-app Browser lobby screenshot.
- Fixed online player-name propagation. Server `start` messages now include the room participant list, the client maps `host` to `p1` and `guest` to `p2`, and `state.players.*.name` is updated before and after remote start synchronization.
- Hardened room-code copy fallback so embedded browsers that disable `prompt()` do not throw an uncaught error.
- Restarted the local `server.mjs` process on port `5174` so the server-side start-sync fix is active in the user's current browser session. Verified with `npm run test:online`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, an online two-page screenshot showing `Google Player`/`Guest Player` in-game, and the required Playwright deck-builder run.
- Added `.env.local` loading to `server.mjs` and saved the provided Google OAuth client ID there so local server restarts keep Google login configured. `.env.local` is ignored by git, and online tests explicitly set `GOOGLE_CLIENT_ID=""` so the unconfigured-auth regression case remains covered.
- Restarted the `5174` server and confirmed `/config` returns `googleSignInEnabled: true`; verified the login screen now shows `Google OAuth: 設定済み`, plus `npm run test:online`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, and the required Playwright deck-builder run.
- Added online turn ownership enforcement. In online matches, `host` controls `p1` and `guest` controls `p2`; end turn, card play, structure play, unit movement, attacks, target resolution, and online state broadcasts are blocked when it is not that client's active turn.
- Added a hand-card confirmation buffer. Clicking a hand card now opens a readable detail overlay with card type, cost, tags, effects, text, and `使う` / `戻る`; non-unit cards resolve only after `使う`, while unit cards enter placement mode only after confirmation.
- Added regression coverage for hand-card buffering and guest attempts to end the host turn. Verified `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node --check tests/online-client-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, a direct Playwright screenshot of the confirmation overlay, and the required Playwright deck-builder run.
- Extended the same confirmation buffer to the Struct Deck. Clicking a structure card now selects it and opens the detail overlay; it is only built after pressing `使う: 建設`.
- Added regression coverage for struct-deck buffering and confirmed the click no longer immediately moves the card into Struct Zone. Verified `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, a direct Playwright screenshot of the struct confirmation overlay, and the required Playwright deck-builder run.
- Added main-deck shuffling at game creation with Fisher-Yates. Normal local/online matches now shuffle each player's main deck before initial draw; Struct Deck remains unshuffled and selectable by design.
- Kept test resets deterministic by calling `createGame(..., { shuffleMainDeck: false })` in `resetForTest`. Verified `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, a five-start sample showing varied opening hands, and the required Playwright deck-builder run.
- Improved rested-card visibility. Rested unit cards now render with a stronger dark overlay, a diagonal gold slash, and a red `REST` badge instead of the previous subtle tint.
- Verified `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, a direct Playwright screenshot comparing rested and normal units, and the required Playwright deck-builder run.
- Fixed a lobby-start sync blocker where `startMatchFromLobby()` refused to start unless a saved deck existed. If no saved deck is selected, the current deck is now normalized and used for the match, allowing the host's lobby start to send the online `start` event.
- Added online regression coverage for starting from the lobby with no saved decks on either browser. Verified against live `5174` with a two-page Playwright reproduction showing the guest entering the game after host start, plus `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node --check tests/online-client-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, and the required Playwright deck-builder run.
- Fixed another host-start sync failure path in the custom WebSocket server. Incoming frames are now buffered across TCP chunks, and outgoing frames support 64-bit payload lengths, so larger match-start states do not get dropped or malformed when sent to the guest.
- Added a large-start online server regression case and restarted the live `5174` server with the fix. Verified `node --check server.mjs`, `node --check tests/online-server-tests.mjs`, `npm run test:online`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, the required Playwright deck-builder run, and a live two-page `5174` reproduction where the guest enters the game and sees `Google Player` / `Guest Player`.
- Added read-only field-card details. Clicking a unit on the Unit Field now opens a full detail overlay without losing the existing action selection, and clicking a built card in Struct Zone opens the same style of detail overlay.
- Added regression coverage for field unit and field struct details. Verified `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, the required Playwright deck-builder run, and screenshots for `field-unit-detail.png` / `field-struct-detail.png`.
- Added viewer-relative board orientation. Online host views `p1` as the near side, online guest views `p2` as the near side, and local hotseat views the active player as the near side. Bottom hand/struct deck and side panels now follow the viewer instead of always following `p1` or the active player.
- Added online regression coverage for perspective ownership and verified that the guest sees `p2` units/hand/core near and `p1` far. Verified `node --check src/main.js`, `node --check tests/online-client-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, the required Playwright deck-builder run, and `guest-perspective-near-side.png`.
- Added opponent card-use reveal overlays. Successful unit summons, tact/wild/grand plays, and struct builds now create a reveal payload with card name, type, cost, tags, effects, and text; the viewer sees it only when the revealed card was used by the opponent, with a local dismiss list to prevent old remote states from reopening a closed reveal.
- Added regression coverage for reveal payload creation and online opponent reveal delivery. Verified `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node --check tests/online-client-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, the required Playwright deck-builder run, and `opponent-card-reveal.png`.
- Added turn-start UI feedback. `startTurn` now records a resource/draw summary, the center board banner shows gained resources and draw count, and the active player's resource boxes show `+N` deltas beside resources gained this turn.
- Added regression coverage for turn-start resource summaries. Verified `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, the required Playwright deck-builder run, and `turn-start-resource-summary.png`.
- Hid opponent pre-confirm card popups. Hand and struct-deck selections now carry `playerId`, and the confirmation popup only appears when the selected card belongs to the current viewer; opponent selections still sync but do not reveal card content until a successful play/build/summon creates the normal reveal payload.
- Made the in-game Struct Deck scrollable. The side panel now shows a bounded two-row Struct Deck window with up/down buttons and mouse-wheel support, backed by `app.structDeckScroll`.
- Added regression coverage for hidden opponent pre-confirm state and Struct Deck scrolling. Verified `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node --check tests/online-client-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, the required Playwright deck-builder run, and `struct-deck-scroll.png`.
- Added Deckmaker interoperability in both directions. The deck builder can import Deckmaker single-deck JSON using `coreCardId`, `mainDeckCardIds`, and `structDeckCardIds`; it can also export the current app's registered cards plus current deck as Deckmaker `deck_data.json` shape `{ cards, decks, worlds }`.
- Deckmaker export maps TWCG card fields into Deckmaker fields: card type, world/faction, tags, play/act costs, ATK/HP, text, keywords, and generated ability text. Unsupported Deckmaker resources such as food are preserved as notes in the description instead of being emitted as unknown cost keys that could break Deckmaker rendering.
- Added regression coverage for Deckmaker deck import and all-data export. Verified `node --check src/main.js`, `node --check tests/keyword-tests.mjs`, `node tests/keyword-tests.mjs http://127.0.0.1:5174`, `npm run test:online`, and the required Playwright deck-builder run showing `Deckmaker読込` / `Deckmaker出力`.
- Unified the old `food` resource into `nature`, including card costs, Wild resource gains, test helpers, and remote-state migration so older states are folded into the new resource model.
- Expanded `Deckmaker読込` to accept full Deckmaker `deck_data.json` payloads. It now registers imported core/main/struct cards persistently, maps Deckmaker resources into TWCG resources, converts known Japanese keywords, maps struct resource generation, and saves imported Deckmaker decks into the local deck list. Verified the provided `C:\Users\haruk\Downloads\deck_data.json` imports as 79 cards and 3 decks.
- Confirmed and covered the online guest-deck start flow: the guest sends their selected/current deck in the join payload, the server stores it on the room client, presence/start payloads include it, and the host uses it as p2's deck when creating the online game. Added server and browser-client regression assertions for guest deck propagation and p2 opening deck/core usage.
- Clarified Deckmaker-imported special effect support: manually registered card abilities and parsed known effects execute, while unrecognized free-text effects remain readable card text until explicitly mapped to an engine effect.
- Fixed an online deck-selection regression where `prepareSelectedDeckForMatch()` could auto-load the first saved deck even when no lobby deck was explicitly selected, causing both players to start from the same deck in some synced-deck states. Room creation/join now freezes the current deck as `hostDeck`/`guestDeck`, and online start restores the host deck for p1 before applying the guest deck to p2. Added a browser regression where saved decks intentionally point at the opposite player's deck while the current decks remain distinct.
- Fixed remaining online sync issues: saved decks are no longer auto-selected on app creation or after server deck sync, and the match lobby no longer visually marks the first saved deck as selected unless the user actually selected it. Turn-end now broadcasts using the player who ended the turn, so the state still syncs after active-player control passes to the opponent. Added browser coverage for host end-turn syncing to the guest.
- Hardened online deck assignment further by deriving both `hostDeck` and `guestDeck` from the server presence/start player list before match start. This prevents local `app.deck`, shared storage, or late deck-sync state from making both p1/p2 use the same deck. Added a regression where the host's local deck is deliberately dirtied to the guest deck immediately before start, while p1 still starts from the host deck.
- Added universal card hover tooltips by registering hover regions in `drawCard()`. Cards rendered in hand, field, deck builder, and zone views now show a compact popup with name, type/faction, stats, tags, cost, keywords, and text on mouse hover. Verified via `output/web-game/hover-card-tooltip.png`.

## TODO

- Add real card image paths to card definitions once user-provided card images are available.
- Set a real Google OAuth client ID in `GOOGLE_CLIENT_ID` and configure authorized origins before using production Google login.
- Replace provisional core card values with final user-supplied core card specs when available.
- Add search/filter/sort inputs to the deck builder once text input UI is introduced for the canvas app.
- Host `server.mjs` or replace it with a production realtime backend before internet play outside the same machine/LAN.
- ngrok free URLs can show the ngrok browser warning page before the game; players may need to proceed past it once.
- Decide the final column count and exact interpretation of "敵ユニットカードの存在する横列のユニットゾーンには配置できない" for all placement cases.
- Add a richer ability timing model: continuous effects, replacement effects, target prompts, and triggered effect queues.
- Define Wild reveal/activation timing and Grand card victory/rule-modification behavior.
- Add player-side enforcement for online matches so each browser can only operate its assigned side.
- Continue expanding the effect model with optional/cancel choices, multiple targets, replacement effects, and continuous effects.

- Fixed Deckmaker-imported core deck-size defaults back to "40?60" so imported cores pass the core template and inherit default start resources/income when Deckmaker data omits explicit resource fields. Restarted the live 5174 server and verified live two-page online play: host and guest decks stayed distinct, and host end-turn synced the guest to p2.
- Corrected the bundled core deckSize strings too, using 40\\u301c60 in source to avoid PowerShell encoding corruption while preserving runtime 40?60. Re-ran keyword tests, online tests, live two-page verification, and the required web-game Playwright screenshot check.
- Fixed Deckmaker core initial resource import: core cards now read initialResources as starting resources and map Deckmaker gold to local unds. Bundled Deckmaker cores with explicit initial resources now refresh over stale localStorage copies, so 肉の王城 starts with funds 7 even if an older imported copy was stored. Added regression coverage for both synthetic initialResources.gold and bundled ���̉���. Verified keyword tests, online tests, live stale-localStorage migration, and required Playwright screenshot check.
- Investigated the repeated host end-turn sync report with real UI clicks on live 5174. Host clicking the END TURN button now moves both host and guest to activePlayer p2 in a two-page Playwright reproduction. Added cache-control: no-store for static files in server.mjs and restarted the live 5174 server so browsers do not keep stale main.js after sync fixes. Verified cache header, real UI end-turn sync, online tests, keyword tests, and required Playwright screenshot check.
- Fixed reported console issues and hardened online sync. Removed aria-hidden from focused hidden inputs, stopped implicit card-image fallback requests for built-in cards so missing mine.png/fieldOrder.png do not generate 404s, and added explicit online state sends after summon/play/build actions instead of relying only on the canvas pointerdown fallback. Verified live UI flow: host hand click -> use -> summon lightInfantry -> guest receives board[3][0], then host END TURN -> guest activePlayer p2, with no aria-hidden or mine/fieldOrder 404 logs. Re-ran online tests, keyword tests, and required Playwright screenshot check.
- Hardened multi-turn online synchronization after a reported 2nd-turn desync. The WebSocket server now assigns monotonically increasing ersion values to every start/state snapshot and includes them in room/start/state payloads. Clients track pp.match.lastStateVersion and ignore stale snapshots, preventing delayed older states from overwriting newer turn progression. Restarted live 5174 and verified host end -> guest turn 1 -> guest end -> host turn 2 -> host end -> guest turn 2 with version 4. Re-ran online tests, keyword tests, and required Playwright screenshot check.
- Added online sync self-healing after screenshots showed each browser stuck on 'opponent turn' with different turn numbers. Server now supports syncRequest to resend room.lastState/version/players to a specific client. Client polls syncRequest during online games, exposes it for testing, applies syncRequest snapshots even when version equals lastStateVersion, and the inactive-turn button requests a sync. Verified: forced local stale state active p1/turn1 after server version 4 active p2/turn2, manual and poll sync both repaired it to p2/turn2. Re-ran online tests, keyword tests, and required Playwright screenshot check.
- Added a server-side online action queue for start/state snapshots. Rooms now enqueue state operations, process them in order, assign monotonically increasing versions, and echo opIds so clients can wait for the authoritative server state before showing local online operations. Client actions that change battle state now use queued sync for summon/play/build, target/choice resolution, movement, attacks, and end turn; pointer clicks restore the pre-action local state until the matching server echo arrives, with a SERVER PROCESSING indicator while pending. Struct builds now close the struct placement selection after one card and show a local confirmation popup. Field units and field structs now open details on double-click only; single-click just selects. Verified with online tests, keyword tests, direct WebSocket queue ordering/version checks, real canvas click tests for single/double-click and struct build popup, and required Playwright screenshot check.
- Implemented the missing Deckmaker-derived effects for 忌地:山, 覆没の大暴走, and クリスタヴィアゴーレム. 忌地:山 now presents a three-way struct-phase choice (魔1/鉱2/金2), 覆没の大暴走 now turns from tact into a struct over 覆没の迷宮 and can pay 鉱2 or 魔1 to summon a cost-3-or-less ゴーレム, and クリスタヴィアゴーレム now requires 覆没の大暴走 to summon and searches/summons a cost-3-or-less ゴーレム to its row on attack. Forced these bundled card definitions to refresh over stale locally imported copies. Verified on a separate 5175 validation server so the current server+Git-site setup was not disturbed: catalog conversion, functional browser scenarios for all three effects, node syntax check, keyword regression tests, online sync tests, and required Playwright screenshot check.
- Fixed GitHub Pages direct access server discovery for the current Cloudflare tunnel. GitHub-hosted pages now default to https://discussing-motivated-personal-forwarding.trycloudflare.com, deriving wss://discussing-motivated-personal-forwarding.trycloudflare.com/ws automatically when ?ws= is absent. Also supports ?server=https://... as a shorter override that persists the derived websocket URL. Confirmed local 5175 and the public /config endpoint both report Google sign-in enabled, then reran syntax, keyword, online, and required Playwright checks.
- Continued the abilities-null card implementation pass. Added stable ID-based bundled refresh for the major previously-null effect cards, connected additional effect handlers and choice resolution, and implemented representative missing effects: 農業協同組合 deploys 農民/農場, 農民 onActivate +1/0, 間接貿易 resource choice plus opponent discard, アトラス・コントロール target control, 産業革命 machine +2/破壊不能, 難民保護施設 enemy-destroy people gain, 忌地 HP-cost activations, 特別指定忌地:霊廟 struct revive/fallback magic, ゴールドラッシュ, 死体漁り, 世論誘導, 動死体の呼び声, 風是の怪異, 怪異災害:吹雪, 思想魔法『強制送還』, and fire-line summon sacrifice requirements. Enemy field cards are tinted red and movement/attack animations now use the current portrait-card field size. Verified syntax, keyword tests, online tests, representative browser smoke tests for the new effects, and required Playwright screenshot check.

- Continued the remaining abilities-null pass. Added ID refresh and runtime handlers for Deus Ex Machina tact people-cost discount, United Kingdom Infantry adjacent pure-human buff, Intelligence Agency opponent tact cancellation, Gold Golem targeted no-counter strike, Improved Margat conditional mobile, Unknown Forbidden Land dump-change life gain, Offering locked rest/unrest/Kaiki revive, Unknown Sad Girl opponent-controlled summon, Unknown Monster awaken/lock restore/core damage, Gusei no Kaii damage redirection, Ikuryu no Kaii one-damage plus upkeep pay/damage, and Flame Great Hero optional destruction payment. Added dump-change notifications for common dump add/remove paths and fixed conditional mobile/double upkeep registration found during verification. Verified node syntax, keyword tests, online tests, catalog browser smoke checks, and the required Playwright screenshot inspection on 5175.

- Continued implementing partial Deckmaker effects. Added Copper Mine machine ore-cost reduction and fixed its structure-phase people-to-ore exchange, made United Kingdom Infantry and Angrona Guard continuous buffs recalculate with board/turn state, implemented Unknown Sad Girl's same-play-cost unit/tact lock, added Zombie from-dump +1/+1, stabilized Ritual Preparation, Research, and Submerged Pulse searches/reveal-pick effects, added Life Counter handling for Life-Line Communication's Rebirth god summon, and completed Wind Kaii's pushback-triggered +1/+1 buff. Verified syntax, keyword tests, online tests, browser catalog smoke checks, and required Playwright screenshot inspection on 5175. Remaining true data gap: �Y�ꂶ�̖��̍��� has no effect text in deck_data.json.

- Replaced the remaining no-UI provisional choice effects with formal in-game choice panels: Life-Line Communication life-counter payment, Flame Great Hero optional destruction payment, and Unknown Monster awaken cost selection. Added resolution helpers and test exports, verified all three pending choices open and resolve in browser, inspected screenshots for life-counter, destruction, and kaiju-awaken panels, and reran syntax, keyword, online, and required Playwright checks on 5175.

- Reworked card/resource UI: copied user-provided resource icons into assets/resources, rendered resources as icons in board/side/top resource displays and card cost areas, highlighted affordable hand/struct-deck cards in yellow, moved core LP/status bars below the core card image, changed destroy and kaiju choice panels to select visible card tiles, and made summoned units enter unrested unless an effect explicitly rests them. Verified syntax, keyword tests, online tests, direct browser screenshots for resource/card choice UI and kaiju choice UI, and the required Playwright deckbuilder screenshot.
- Finalized resource/card UI follow-up: restricted unit placement clicks to unit-field cells after the summon-row layout split, reran syntax/keyword/online tests, and verified the required Playwright deckbuilder flow plus card-tile choice screenshots.
